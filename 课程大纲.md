# 前端监控全栈实战 · 课程大纲

> 最后更新：2026-05-06

---

## 课程文件组织规范

### 目录结构

每一章对应一个独立文件夹，按章节编号命名，内含两个子目录：

```
/Users/yingside/work/bak/monitor/
├── 课程大纲.md
├── 01.课程介绍与前端监控全景/
│   ├── 课件/                   # 本章课件（Markdown）
│   └── 代码/                   # 本章截止时的完整项目代码快照
├── 02.需求分析与系统架构设计/
│   ├── 课件/
│   └── 代码/
│       └── monitor/            # 本章结束后的项目快照
│           ├── packages/
│           ├── apps/
│           └── ...
├── 03.Monorepo工程化搞建(一)/
│   ├── 课件/
│   └── 代码/
│       └── monitor/
... （依次类推）
```

### 代码连贯性规范

> ⚠️ **重要**：本课程的项目代码是**一个不断增长的 Monorepo 项目**，并不是每章独立的新项目。

- **并不是每章新建**：每章的代码是在上一章**的基础上继续添加 / 修改**，最终形成一个完整可运行的项目
- **代码快照**：每章 `代码/` 目录内放的是**本章讲解内容完成后，项目当前状态的完整快照**，供学生对照参考
- **连贯性检查**：每章代码生成前，必须先读取上一章的代码状态，确保新增内容与已有代码完全兼容，不破坏已完成的功能
- **增量实现**：课件和大纲中描述的每个步骤，均在已有代码基础上增量实现，不重复已生成过的代码
- **不可倒退原则**：如本章代码需要修改上一章的内容，必须明确说明修改点和原因，不允许静默删改之前的功能

---

### 代码风格规范

> 以下规范适用于本 Monorepo 中所有涉及前端的代码，包括：前端监控平台（`apps/frontend/monitor/`）、demos 示例项目（`demos/`）、SDK 包（`packages/`）。后端代码（NestJS）单独说明。

#### 通用规范（全项目）

- 语言：全面使用 **TypeScript**，禁止使用 `any`（SDK 类型边界处可用 `unknown` 替代）
- 模块规范：统一使用 **ES Module**（`import` / `export`），禁止 `require`
- 缩进：**2 空格**
- 引号：**单引号**（`'`）
- 末尾分号：**不加分号**（配合 Prettier 统一处理）
- 命名规范：
  - 组件文件：`PascalCase`（如 `ErrorTable.tsx`）
  - 工具函数/变量：`camelCase`
  - 常量：`UPPER_SNAKE_CASE`
  - 类型/接口：`PascalCase`，接口不加 `I` 前缀（如 `type ErrorLog = ...`）

#### 前端监控平台 & demos（React 端）

- **组件**：函数式组件 + Hooks，禁止 Class 组件
- **样式**：全部使用 **Tailwind CSS 工具类**，禁止写内联 `style` 和独立 CSS 文件（特殊动画除外）
- **UI 组件**：优先使用 **shadcn/ui** 中已有组件，不重复封装同功能组件
- **图标**：统一使用 **Lucide React**，不引入其他图标库
- **图表**：统一使用 **Recharts**
- **状态管理**：组件内状态用 `useState` / `useReducer`，跨组件全局状态用 **Zustand**，禁止 props 层层透传超过 2 层
- **路由**：使用 **react-router-dom v6**，路由表统一在 `router/index.tsx` 中维护
- **HTTP 请求**：统一使用封装后的 Axios 实例（`utils/request.ts`），禁止在组件中直接调用原始 `axios`
- **路径别名**：统一使用 `@/` 指向 `src/`，禁止使用相对路径 `../../` 超过 2 层

#### demos 示例项目（Vue 端，若有）

- 使用 **Vue 3 Composition API**（`<script setup>` 语法），禁止 Options API
- 样式同样使用 **Tailwind CSS**，风格与 React 端保持视觉一致
- 路由使用 **vue-router v4**

#### SDK 包（`packages/`）

- 纯 TypeScript，**不依赖任何 UI 框架**（`core` / `browser` / `browser-utils` 包）
- 每个包的对外 API 必须有完整的 **TypeScript 类型声明**
- 框架适配包（`vue` / `react`）只做胶水层，不包含业务逻辑
- 不引入任何非必要的第三方依赖，保持包体积最小

#### 后端（NestJS）

- 文件命名：`kebab-case`（如 `error-log.service.ts`）
- 每个模块目录包含：`*.module.ts` / `*.controller.ts` / `*.service.ts` / `*.dto.ts`
- DTO 使用 `class-validator` 装饰器做入参校验
- 响应格式统一为 `{ code: number, data: unknown, message: string }`

---

### UI 设计风格参考

> 设计参考文件存放于：
> - `apple/DESIGN.md`：Apple 官网风格设计系统
> - `sentry/DESIGN.md`：Sentry 开发者工具风格设计系统
>
> 实现时以 **shadcn/ui + Tailwind CSS + Lucide** 为基础组件层，在此之上应用对应的设计风格。不需要完全照搬，**设计精神优先于像素还原**。

#### demos 示例项目 → 参考 Apple 设计（`apple/DESIGN.md`）

Apple 风格核心关键词：**极简、留白、产品优先、光线感**

| 设计要素 | 参考值 |
|---------|--------|
| **主色** | Action Blue `#0066cc`，深色模式下用 `#2997ff` |
| **背景** | 浅色：`#ffffff` / `#f5f5f7`；深色 tile：`#272729` |
| **文字色** | 主色 `#1d1d1f`，次要 `#7a7a7a`，深色背景上 `#ffffff` |
| **字体** | `SF Pro Display`（大标题） / `SF Pro Text`（正文），无 SF Pro 时降级 `system-ui, -apple-system` |
| **大标题** | 56px / weight 600 / letter-spacing -0.28px，负字间距营造紧凑感 |
| **正文** | 17px / weight 400 / line-height 1.47 |
| **圆角** | 偏小：按钮 `8px`，卡片 `12px`，图片容器 `18px` |
| **阴影** | 仅产品图片有阴影，UI 元素本身无装饰阴影 |
| **禁止** | 装饰性渐变、UI chrome 上的阴影、多余色彩 |
| **整体感** | 大量留白、边缘对齐严格、内容自己会说话 |

#### 监控平台 → 参考 Sentry 设计（`sentry/DESIGN.md`）

Sentry 风格核心关键词：**暗色开发者工具、紫罗兰氛围、代码编辑器既视感**

| 设计要素 | 参考值 |
|---------|--------|
| **主背景** | 深紫黑 `#1f1633`，次级背景 `#150f23`，**非纯黑** |
| **边框/分割线** | `#362d59` |
| **主交互色** | Sentry Purple `#6a5fc1`，Muted Purple `#79628c` |
| **强调色** | Lime Green `#c2ef4e`（高可见度 CTA、高亮徽标） |
| **辅助色** | Coral `#ffb287`（焦点态背景），Pink `#fa7faa`（焦点轮廓） |
| **字体** | 主 UI 字体 `Rubik`（回落 `-apple-system, system-ui`），代码用 `Monaco / Menlo` |
| **标题** | Rubik 24–30px / weight 500–600，行高 1.20–1.25（紧凑） |
| **正文** | Rubik 16px / weight 400 / line-height 1.50 |
| **标签/按钮** | 全部 `uppercase` + letter-spacing 0.2px，营造"技术标签"系统感 |
| **按钮** | 内嵌阴影 `inset rgba(0,0,0,0.1)` 营造触感，圆角 13px |
| **卡片** | 半透明深紫表面，radius 8–12px，毛玻璃 `blur(18px) saturate(180%)` |
| **纵向间距** | section 间 64–80px+，大量深色"呼吸空间" |
| **禁止** | 纯黑背景、冷灰色调、扁平无层次的界面 |
| **整体感** | 深夜调试氛围，内容像从深紫背景中发光而出 |

---

## 课程定位

从零打造一套完整的企业级前端监控系统，理论与实操并重。  
覆盖：监控 SDK 设计 → 数据上报 → Kafka 消息队列 → 后端服务 → 数据存储 → 可视化平台 → 应用容器化部署。  
架构思路参考 Sentry，但全部独立实现，不依赖任何有版权争议的第三方框架代码。

> **面向学员**：本课程面向有前端基础的学员，核心目标是培养**架构思维**。每章课件须遵守以下原则：
>
> 1. **架构思考必须显式体现**：每引入一项新技术或新设计决策，必须加 `> 🏗️ **架构思考**` callout，解释"为什么这样设计"，而不只是"怎么用"。
> 2. **术语先白话后术语**：课件中术语首次出现时，用 `> 📖 **术语：XXX**` 给出白话解释，避免只给抽象定义。
> 3. **选型不固化**：第一章技术选型章节不应将具体产品写死，保留备选空间，在真正使用该技术的章节再确定。

> **Docker 使用说明**：本课程中 Docker 分两个阶段使用：
> - **开发阶段（第 12 章起）**：用 `docker-compose.yml` 在本地启动基础设施服务（Kafka / 列式分析库 / PostgreSQL），NestJS 后端和 React 前端仍以 `pnpm dev` 本机运行，方便调试。
> - **部署阶段（第 23 章）**：将后端服务和前端应用本身也容器化，实现一键启动完整系统。

---

## 技术选型总览

| 层次 | 技术栈 |
|------|--------|
| **Monorepo 管理** | pnpm workspace + Turborepo |
| **SDK 语言** | TypeScript |
| **SDK 适配** | 原生 JS / Vue3 / React |
| **示例项目** | Vite + Vue3 / Vite + React（demos 目录，具体按实践确定） |
| **数据上报** | fetch / Navigator.sendBeacon |
| **后端框架** | NestJS + TypeScript |
| **消息队列** | Apache Kafka（Docker 部署，实际演示） |
| **监控数据库** | **ClickHouse**（列式 OLAP，单容器，Node.js 官方驱动 `@clickhouse/client`） |
| **业务数据库** | PostgreSQL（关系型，用于用户/项目管理） |
| **ORM** | TypeORM（PostgreSQL） |
| **列式库接入** | `@clickhouse/client`（官方 Node.js 客户端）|
| **容器化** | Docker + docker-compose |
| **前端平台** | Vite + React + shadcn/ui + Tailwind CSS + Lucide |
| **图表** | Recharts |
| **全局状态** | Zustand |
| **HTTP 客户端** | Axios（封装拦截器） |

---

## 最终项目结构

```
monitor/                              # pnpm monorepo 根目录
├── packages/
│   ├── core/                         # SDK 核心（框架无关逻辑）
│   ├── browser/                      # 浏览器 SDK 入口包
│   ├── browser-utils/                # 浏览器工具函数包
│   ├── vue/                          # Vue3 插件适配包
│   └── react/                        # React 适配包
├── apps/
│   ├── backend/
│   │   ├── dsn-server/               # 数据接收服务（NestJS → Kafka → ClickHouse）
│   │   └── monitor-server/           # 平台 API 服务（NestJS + PostgreSQL）
│   └── frontend/
│       └── monitor/                  # 可视化平台（Vite + React + shadcn/ui）
├── demos/                            # 各框架接入示例（具体按实践确定）
├── docker/
│   └── docker-compose.yml            # 【开发环境基础设施】Kafka + ClickHouse + PostgreSQL 容器编排
├── pnpm-workspace.yaml
└── turbo.json
```

---

## 章节大纲

---

### 第 01 章：课程介绍与前端监控全景

> 目标：理解前端监控的价值、边界与体系，建立全局视野

- 1.1 从线上故障说起——前端监控能解决什么问题
- 1.2 前端监控的三大核心能力：数据采集 / 数据存储 / 数据展示
- 1.3 主流监控平台横向对比（Sentry / 阿里 ARMS / 灯塔 / 神策）
- 1.4 Sentry 快速体验：注册账号、接入 SDK、触发错误、查看上报
- 1.5 从 Sentry 反推：一套监控系统由哪些部分组成
- 1.6 本课程要做什么——完整架构图与各模块职责说明
- 1.7 课程约定：技术选型说明与为何选择当前技术栈

---

### 第 02 章：需求分析与系统架构设计

> 目标：将监控系统需求拆解为可落地的技术方案

- 2.1 监控系统功能需求梳理
  - 错误监控：JS 错误 / 资源错误 / Promise 异常 / 框架层错误
  - 性能监控：Core Web Vitals / 页面加载时序 / 资源耗时
  - 用户行为：PV/UV / 页面停留 / 路由跳转 / 点击埋点
  - API 监控：请求成功率 / 耗时 / 异常响应
- 2.2 数据流转链路设计：SDK → 上报 → DSN 服务 → Kafka → 消费写入 ClickHouse
- 2.3 业务后端设计：用户认证 / 项目管理 / 数据查询 API
- 2.4 前端平台功能规划：登录 / 项目管理 / 各类数据看板
- 2.5 数据库分工说明：PostgreSQL 管理业务数据，ClickHouse 存储分析型监控数据
- 2.6 DSN 设计：什么是 DSN，如何通过 DSN 关联上报来源
- 2.7 完整系统架构图绘制与讲解

---

### 第 03 章：Monorepo 工程化搭建（一）——基础结构

> 目标：用 pnpm workspace 搭建多包项目骨架

- 3.1 Monorepo 是什么，适合监控系统的理由
- 3.2 pnpm workspace 初始化：`pnpm-workspace.yaml` 配置
- 3.3 创建 `packages/` 和 `apps/` 目录，初始化各子包 `package.json`
- 3.4 子包命名规范（`@monitor/core`、`@monitor/browser` 等）
- 3.5 子包互相依赖的引用方式（workspace 协议 `workspace:*`）
- 3.6 根目录 `package.json` 的公共脚本设计
- 3.7 TypeScript 多包配置
  - `tsconfig.base.json`：公共编译选项
  - `tsconfig.app.json`：前端客户端配置
  - `tsconfig.server.json`：后端 Node/NestJS 配置
  - 子包的 `tsconfig.json` 如何继承根配置

---

### 第 04 章：Monorepo 工程化搭建（二）——规范与构建加速

> 目标：统一代码规范，用 Turborepo 加速构建

- 4.1 ESLint 统一规范配置（`eslint.config.mjs` 根配置 + 子包继承）
- 4.2 Prettier 格式化配置与 ESLint 协同
- 4.3 `.gitignore` 与 `.npmrc` 的 monorepo 最佳实践
- 4.4 Turborepo 是什么，能解决什么问题（任务调度 / 缓存加速）
- 4.5 `turbo.json` pipeline 配置：`build` / `dev` / `lint` 任务依赖关系
- 4.6 常用开发命令整合（根目录 `dev` 启动全部服务）
- 4.7 验证：跑通 monorepo 下各包的构建流程

---

### 第 05 章：SDK 架构设计——整体思路与核心模块

> 目标：参考 Sentry 的分层架构，设计我们自己的 SDK 结构

- 5.1 Sentry SDK 架构分析：Hub / Client / Transport / Integration 四层模型
- 5.2 我们的 SDK 分层设计
  - `core` 包：框架无关的核心逻辑（初始化 / 数据流转 / 插件管理）
  - `browser` 包：浏览器环境入口，组合各采集能力
  - `browser-utils` 包：纯工具函数（UUID / 元素路径解析 / 设备信息等）
  - `vue` / `react` 包：框架适配层
- 5.3 `init` 方法的设计：配置项（dsn / appId / userId / 采样率 / 插件列表）
- 5.4 插件（Plugin）机制设计：统一的插件注册接口与生命周期
- 5.5 数据管道（Pipeline）设计：采集 → 处理 → 缓冲 → 上报
- 5.6 在 demos 项目中验证 SDK 的基本接入流程

---

### 第 06 章：SDK 采集——JS 错误与资源加载错误

> 目标：实现运行时错误和静态资源错误的完整采集

- 6.1 JS 运行时错误的两种捕获方式对比
  - `window.onerror`：参数含义，为什么不推荐
  - `window.addEventListener('error', handler, true)`：推荐方案
- 6.2 静态资源加载失败捕获（`<img>` / `<script>` / `<link>` 错误）
- 6.3 如何区分 JS 运行时错误和资源加载错误（`e.target` 判断）
- 6.4 错误信息结构化处理
  - 错误类型 / 消息 / 文件 / 行列号
  - StackTrace 原始字符串的提取
- 6.5 SourceMap 预留说明（后续章节决定是否实现还原）
- 6.6 SyntaxError 为什么无法捕获
- 6.7 将错误采集封装为独立的 Plugin

---

### 第 07 章：SDK 采集——Promise 异常与框架层错误

> 目标：覆盖异步异常和 Vue3/React 框架内部错误

- 7.1 `unhandledrejection` 事件：Promise 未捕获异常的监听
- 7.2 `rejectionhandled` 事件补充说明
- 7.3 Vue3 框架层错误接入
  - `app.config.errorHandler` 的用法
  - 与全局 `addEventListener` 的关系与区别
- 7.4 React 框架层错误接入
  - ErrorBoundary 组件封装
  - `componentDidCatch` 与 `getDerivedStateFromError`
- 7.5 多框架下的错误归一化处理（统一为同一种数据结构上报）
- 7.6 `vue` 和 `react` 适配包的具体实现
- 7.7 错误去重策略（相同 StackTrace 的合并，防止刷屏上报）

---

### 第 08 章：SDK 采集——页面性能指标

> 目标：采集 Web Vitals 与 Navigation Timing，量化页面性能

- 8.1 性能为什么是相对的——指标比感觉更可靠
- 8.2 浏览器导航时序模型（Navigation Timing Level 2 规范解读）
  - DNS / TCP / TTFB / DOM 解析 / 资源加载各阶段耗时计算
- 8.3 Web Vitals 核心指标详解
  - FCP（First Contentful Paint）
  - LCP（Largest Contentful Paint）
  - CLS（Cumulative Layout Shift）
  - INP（Interaction to Next Paint，替代 FID）
  - TTFB（Time to First Byte）
- 8.4 使用 `PerformanceObserver` 原生采集 LCP / CLS / INP
- 8.5 集成 `web-vitals` 库（对比原生采集的优缺点）
- 8.6 资源加载性能采集（`getEntriesByType('resource')`）
- 8.7 Lighthouse 实战：解读 Performance 评分、Opportunities 与 Diagnostics
- 8.8 将性能采集封装为独立的 Plugin

---

### 第 09 章：SDK 采集——用户行为与埋点

> 目标：实现 PV/UV 统计、路由追踪、点击行为采集

- 9.1 埋点三种方案对比：代码埋点 / 全埋点（无埋点）/ 可视化埋点
- 9.2 本课选择方案说明：全埋点为主，代码埋点补充关键业务节点
- 9.3 PV/UV 统计原理与实现（每次进入页面上报 PV，UV 由服务端处理）
- 9.4 页面停留时长采集（`visibilitychange` + `beforeunload`）
- 9.5 SPA 路由变更监听
  - `popstate` 与 `hashchange` 事件
  - `history.pushState` / `replaceState` 的劫持
- 9.6 Vue3 Router 钩子集成（`router.afterEach`）
- 9.7 React Router 集成方案
- 9.8 点击行为采集（事件委托 + DOM 元素路径（XPath/CSS Selector）提取）
- 9.9 用户行为序列存储（行为栈设计，为错误复现提供上下文）

---

### 第 10 章：SDK 采集——API 请求监控

> 目标：无侵入拦截 XHR 与 Fetch，采集接口耗时与异常

- 10.1 为什么要监控 API：接口失败是 "无声的前端 Bug"
- 10.2 劫持 `XMLHttpRequest`
  - 重写 `open` / `send` / `onreadystatechange`
  - 记录 url / method / requestBody / status / duration
- 10.3 劫持 `fetch`
  - 用 Proxy 包装原生 `fetch`
  - Promise 链中采集 response 状态和耗时
- 10.4 过滤上报请求本身（防止死循环，通过 DSN 地址判断）
- 10.5 采集数据结构设计：`{ url, method, status, duration, requestId }`
- 10.6 将 API 监控封装为独立的 Plugin

---

### 第 11 章：SDK 上报——数据结构设计与上报策略

> 目标：设计统一的上报数据格式，实现可靠、高效的数据上报

- 11.1 上报数据通用结构设计

  ```typescript
  {
    traceId: string,     // 本次会话唯一标识
    appId: string,       // 项目标识
    userId?: string,     // 用户标识（可选）
    type: 'error' | 'performance' | 'behavior' | 'api',
    payload: object,     // 具体数据
    timestamp: number,
    page: string,        // 当前页面 URL
    ua: string           // User-Agent
  }
  ```

- 11.2 `traceId` / `spanId` 的生成（UUID 工具函数实现）
- 11.3 上报方式对比
  - `fetch` POST：常规上报
  - `Navigator.sendBeacon`：页面卸载时的可靠上报
  - 何时选用哪种方式
- 11.4 上报优化策略
  - 节流与批量上报（缓冲队列，定时刷新）
  - 离线缓存（IndexedDB 方案预留）
  - 采样率控制（`sampleRate` 配置项）
- 11.5 跨域问题处理（CORS 配置说明）
- 11.6 在 demos 项目中完整验证 SDK 的端到端采集与上报

---

### 第 12 章：基础设施——本地开发环境搭建（Docker）

> 目标：用 Docker Desktop + Docker Compose 在本地一键启动开发所需的基础设施（Kafka / ClickHouse / PostgreSQL）
>
> ❗ **课程深度说明**：本课程 Docker 仅做为开发环境工具使用，重点是会用 Docker Desktop 启动服务、理解各服务的作用。Docker 本身的原理、网络模型、镜像制作等进阶内容不展开。

- 12.1 Docker Desktop 安装与基本使用（镜像 / 容器 / Volume 核心概念）
- 12.2 本课 Docker 的定位：**只跑基础设施，不跑应用本身**
- 12.3 ClickHouse 是什么（白话：专门用来做大量数据分析的数据库）
  - OLAP vs OLTP 场景区分（看销售报表 vs 处理订单的类比）
  - 行式存储 vs 列式存储：为什么监控数据适合列式存储
  - 为什么选 ClickHouse：单容器、轻量、Node.js 官方驱动成熟、学习资料丰富
- 12.4 Apache Kafka 是什么（白话：外卖系统的「中转调度台」）
  - 消息队列解决什么问题：监控上报量峰值时数据库来不及写入怎么办
  - 核心概念简介：Topic（频道）/ Producer（生产者）/ Consumer（消费者）
- 12.5 编写 `docker/docker-compose.yml`（随项目代码维护）
  - Kafka（KRaft 模式，无需 Zookeeper，使用 apache/kafka:3.9.2 官方镜像）
  - Kafka UI（可视化监控 Topic，端口 8081）
  - ClickHouse（单容器，端口 8123 HTTP / 9000 Native）
  - PostgreSQL（端口 5432）
- 12.6 封装常用命令到根目录 `package.json`：`infra:start` / `infra:stop` / `infra:reset` / `infra:logs`
- 12.7 用 Docker Desktop 界面验证容器启动状态
- 12.8 DataGrip 连接验证（连接 PostgreSQL 和 ClickHouse，执行简单查询验证连通性）

---

### 第 13 章：ClickHouse 数据表设计

> 目标：为四类监控数据建表，并验证数据写入与查询
>
> ❗ **课程深度说明**：重点是建出能用的表结构，理解 ClickHouse MergeTree 系列引擎的基本选择逻辑即可，ClickHouse 性能调优、物化视图等进阶内容不展开。

- 13.1 ClickHouse 建表语法快速上手（`CREATE TABLE`，常用数据类型，`ENGINE = MergeTree()`）
- 13.2 MergeTree 系列引擎简介与选择依据
  - MergeTree：基础引擎，适合时序日志（监控数据的首选）
  - ReplacingMergeTree：相同主键自动去重（UV 统计场景）
  - AggregatingMergeTree：物化聚合（高级用法，本课程不展开）
- 13.3 四类监控表结构设计（以 `app_id` 和时间为排序键，按月分区）
  - `error_logs`：错误日志表
  - `performance_logs`：性能日志表
  - `behavior_logs`：用户行为表
  - `api_logs`：API 请求表
- 13.4 在 DataGrip 中执行建表语句并验证
- 13.5 使用 ClickHouse HTTP API 手动测试写入一条数据（`INSERT INTO ... FORMAT JSONEachRow`）
- 13.6 常用聚合查询 SQL 实践（错误按时间分组统计 / PV 汇总 / 按 appId 筛选）

---

### 第 14 章：后端——DSN 数据接收服务

> 目标：用 NestJS 创建 `/report` 接口，接收 SDK 上报的数据并转入 Kafka
>
> ❗ **课程深度说明**：本章不展开 NestJS 全量 API，至少需要看过 NestJS 基础课程再学本课。重点在于连通整个数据链路，而不是 NestJS 本身的深度讲解。

- 14.1 在 Monorepo 中创建 `dsn-server` NestJS 子应用
- 14.2 设计 `/report` 上报端点（POST 接收，支持批量数据）
- 14.3 简单的 Body 数据校验（`class-validator` + DTO）
- 14.4 appId 合法性校验（判断上报来源是否在已建项目中）
- 14.5 接口速率限制（`@nestjs/throttler`，防止大量无效请求将服务冲垓）
- 14.6 将数据写入 Kafka Producer（按 `type` 分发到不同 Topic）
  - `monitor.error` / `monitor.performance` / `monitor.behavior` / `monitor.api`
- 14.7 在 demos 项目中验证接收链路（Postman 或直接连 SDK 跟踪）

---

### 第 15 章：后端——Kafka 消费与 ClickHouse 写入

> 目标：实现 Kafka Consumer，消费消息并写入 ClickHouse
>
> ❗ **课程深度说明**：Kafka 的复杂特性（分区负载均衡、副本高可用等）不展开。涉及的工程实践局限于：定义 Topic、写 Producer、写 Consumer，实现数据流通即可。

- 15.1 快速回顾 Kafka 消费模型：Consumer Group + Offset 的工作方式
- 15.2 在 NestJS 中集成 Kafka Consumer（`@nestjs/microservices`）
- 15.3 四个 Topic 的 Consumer 实现
  - 消费 `monitor.error` → 写入 ClickHouse `error_logs`
  - 消费 `monitor.performance` → 写入 ClickHouse `performance_logs`
  - 消费 `monitor.behavior` → 写入 ClickHouse `behavior_logs`
  - 消费 `monitor.api` → 写入 ClickHouse `api_logs`
- 15.4 封装 ClickHouse HTTP 写入（`@clickhouse/client` INSERT 方法，批量写入）
- 15.5 用 Kafka UI 可视化查看 Topic 消费进度（验证消息流通）
- 15.6 联调验证：SDK 上报 → DSN 服务 → Kafka → Consumer → ClickHouse

---

### 第 16 章：后端——监控平台 API 服务

> 目标：用 NestJS + PostgreSQL 构建可视化平台的业务后端
>
> ❗ **课程深度说明**：后端的重点是为前端平台提供可用的接口。JWT 鉴权、TypeORM 等内容点到为止，不做深入演讲。

- 16.1 在 Monorepo 中创建 `monitor-server` NestJS 子应用
- 16.2 TypeORM 集成 PostgreSQL（数据源配置 + Entity 定义）
- 16.3 PostgreSQL 表结构：`users` 用户表 + `projects` 项目表（含 `app_id` / `dsn`）
- 16.4 用户认证：注册 / 登录接口 + JWT 生成 + `JwtAuthGuard` 守卫
- 16.5 项目管理 CRUD：创建项目 → 自动生成 `appId` + `dsn`
- 16.6 数据查询接口：从 ClickHouse 聚合查询下列数据
  - 错误趋势 / 错误列表（分页+筛选）/ 错误详情
  - Core Web Vitals 分布 / PV/UV 趋势 / API 异常率 Top 10
- 16.7 接口统一响应格式（拦截器：`{ code, data, message }`）
- 16.8 Swagger 自动生成接口文档（自我描述 + 前端调试用途）

---

### 第 17 章：前端平台——工程搭建与基础架构

> 目标：搭建 React + shadcn/ui 可视化平台的工程基础

- 17.1 在 Monorepo 中用 Vite 创建 React + TypeScript 前端应用
- 17.2 集成 Tailwind CSS（配置 `tailwind.config.ts`）
- 17.3 集成 shadcn/ui（按官方 Vite 文档操作，处理路径别名 `@/*`）
  - 注意：`tsconfig.app.json` 中 paths 配置（不使用 baseUrl）
  - 在 monorepo ESLint 中忽略 `components/ui/` 目录
- 17.4 Lucide 图标库基本使用
- 17.5 react-router-dom v6 路由规划

  ```
  /login                   登录页
  /register                注册页
  /:projectId/overview     项目概览
  /:projectId/errors       错误监控
  /:projectId/performance  性能监控
  /:projectId/behavior     用户行为
  /:projectId/api          API 监控
  /projects                项目管理
  /settings                账户设置
  ```

- 17.6 Axios 封装（请求拦截器注入 Token / 响应拦截器统一错误处理 / 401 自动跳转登录）
- 17.7 Zustand 全局状态设计（用户信息 store / 当前项目 store）
- 17.8 整体布局组件（Sidebar + Header + 主内容区）

---

### 第 18 章：前端平台——登录、注册与项目管理

> 目标：实现用户认证流程和项目管理功能

- 18.1 登录页面（shadcn/ui Card + Form + Input 组件，表单校验）
- 18.2 注册页面
- 18.3 JWT Token 的存储与管理（`localStorage` 方案 + 安全注意事项）
- 18.4 路由守卫（未登录跳转 `/login`）
- 18.5 项目列表页（Card 网格布局，项目概要信息展示）
- 18.6 创建项目弹窗（Dialog 组件，填写项目名称 → 生成 DSN）
- 18.7 SDK 接入引导页（展示 DSN，展示 npm 安装命令和初始化代码）
- 18.8 项目切换（Sidebar 下拉选择，切换后更新全局 project store）

---

### 第 19 章：前端平台——错误监控看板

> 目标：实现完整的错误数据可视化

- 19.1 错误趋势折线图（Recharts `LineChart`，按时间维度切换：24h / 7d / 30d）
- 19.2 错误概览统计卡（总错误数 / 影响用户数 / 错误率，shadcn/ui Card）
- 19.3 错误列表表格
  - shadcn/ui `Table` 组件
  - 列：错误类型 / 消息 / 首次出现 / 最近出现 / 发生次数 / 状态
  - 筛选（错误类型下拉）+ 搜索（关键词）+ 分页
- 19.4 错误详情页
  - StackTrace 展示（等宽字体，代码块格式）
  - 影响用户列表
  - 相关用户行为序列（还原错误前的操作路径）
  - 错误趋势迷你图

---

### 第 20 章：前端平台——性能监控看板

> 目标：实现 Core Web Vitals 和页面性能数据可视化

- 20.1 Core Web Vitals 评分卡（LCP / CLS / INP / FCP / TTFB）
  - 指标评级可视化（Good / Needs Improvement / Poor 三档，颜色区分）
- 20.2 性能指标分布图（Recharts `BarChart`，P75 / P90 / P99 分位数展示）
- 20.3 页面加载时序瀑布图（DNS / TCP / TTFB / DOMParse / Load 各阶段）
- 20.4 最慢页面 Top 10（表格，按 LCP 或 FCP 排序）
- 20.5 Lighthouse 评分趋势（如果有历史数据）

---

### 第 21 章：前端平台——用户行为与 API 监控看板

> 目标：实现 PV/UV 分析和 API 监控看板

- 21.1 PV/UV 趋势折线图（双轴，PV 与 UV 同时展示）
- 21.2 热门页面 Top 10（表格，按 PV 降序）
- 21.3 用户地域分布（简单表格或简单地图组件）
- 21.4 API 监控看板
  - 请求成功率趋势
  - 慢请求 Top 10（按平均耗时排序）
  - 异常接口列表（4xx / 5xx 错误接口）
- 21.5 日期范围选择组件（shadcn/ui DatePickerWithRange，全局联动所有图表）

---

### 第 22 章：端到端联调与测试

> 目标：将所有模块串联，从 SDK 上报到平台展示完整跑通

- 22.1 在 demos 项目中集成完整 SDK（配置 dsn / appId）
- 22.2 手动触发各类监控事件
  - 抛出 JS 运行时错误
  - 触发 Promise 未捕获异常
  - 模拟慢 API 请求
  - 路由跳转行为记录
- 22.3 验证完整链路：SDK → DSN → Kafka → Consumer → ClickHouse → API → 前端平台
- 22.4 SourceMap 取舍决策（根据课程进度决定是否实现还原，本章预留接入点）
- 22.5 常见问题排查清单
  - 跨域（CORS 配置检查）
  - Kafka 消费延迟排查
  - ClickHouse 写入失败排查
  - 前端数据为空排查思路

---

### 第 23 章：应用容器化部署与课程总结

> 目标：在第 12 章基础设施容器的基础上，将 NestJS 后端和 React 前端应用本身也容器化，完成完整的一键启动部署方案
>
> ⚠️ **课程深度说明**：本章只做"跑通本地完整部署"的演示，不涉及生产级运维（负载均衡、日志收集、监控告警等）。Docker 相关内容仍以 Docker Desktop 可视化操作为主，辅以少量命令行验证。

- 23.1 回顾开发环境 vs 生产环境的 Docker 使用差异
- 23.2 为 `dsn-server` 编写 Dockerfile（多阶段构建）
  - Stage 1：`node:20-alpine` 安装依赖 + 构建 `dist/`
  - Stage 2：只复制 `dist/` 和 `node_modules` 运行
- 23.3 为 `monitor-server` 编写 Dockerfile（同上）
- 23.4 为 React 前端编写 Dockerfile（Vite build + Nginx 静态托管，处理 SPA 路由 404）
- 23.5 在 `docker/docker-compose.yml` 中追加应用服务
  - 已有：Kafka / ClickHouse / PostgreSQL
  - 新增：dsn-server / monitor-server / monitor-frontend（Nginx）
  - 服务启动顺序依赖（`depends_on` + `healthcheck`）
- 23.6 环境变量管理
  - `.env` 文件规范（数据库连接串 / Kafka 地址 / JWT Secret 等）
  - `.env.example` 提交到 Git，`.env` 加入 `.gitignore`
  - Docker Compose 中的变量注入方式
- 23.7 本地完整运行验证（一条命令拉起全部服务）
- 23.8 课程总结：完整架构回顾图
- 23.9 后续延伸方向
  - SourceMap 自动上传与错误定位到源码
  - CI/CD 流水线集成（GitHub Actions）
  - 告警系统（钉钉 / 企业微信 Webhook）
  - Kafka 多 Partition 扩容与高可用
  - 多租户架构改造
  - 接入 Nginx 访问日志采集

---

## 章节数量汇总

| 模块 | 章节 |
|------|------|
| 课程概述与需求分析 | 第 01 ~ 02 章 |
| Monorepo 工程化 | 第 03 ~ 04 章 |
| SDK 设计与各类采集 | 第 05 ~ 11 章 |
| 基础设施（Docker / Kafka / ClickHouse / PostgreSQL） | 第 12 ~ 13 章 |
| 后端服务（DSN + Kafka + 业务 API） | 第 14 ~ 16 章 |
| 前端可视化平台 | 第 17 ~ 21 章 |
| 联调测试与部署 | 第 22 ~ 23 章 |
| **合计** | **23 章** |
